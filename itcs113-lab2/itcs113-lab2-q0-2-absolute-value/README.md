# Absolute Value

Write a program that reads one integer `x` and prints its absolute value.

- If `x` is negative, output `-x`.
- Otherwise, output `x`.

**Input**: one integer `x`.

**Output**: the absolute value of `x`.

<hr>

Sample input:
```
-7
```

Sample output:
```
7
```
